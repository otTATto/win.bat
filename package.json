{
  "scripts": {
    "format": "prettier --write \"**/*.{md,json}\""
  },
  "lint-staged": {
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "prettier": "^3"
  }
}
